on:
  push:
    branches:
      - ActionTest


jobs:
  createPullRequest:
    runs-on: ubuntu-latest
    steps:
      - name: create pull request
        run: gh pr create -B main -H ActionTest --title 'Merge branch_to_merge into base_branch' --body 'Created by Github action'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}